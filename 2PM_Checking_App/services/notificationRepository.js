// Stubbed notification repository for future use.
// Current milestone: no-op implementations so drawings feature does not depend on notifications.

export async function createNotificationForSiteUsers(_siteId, _payload) {
  // Intentionally empty – reserved for future implementation.
  return;
}

